- quiniela = participacion.quiniela
div.twelve.columns.white-background.rounded-corners
    div
        h4.ten.columns.offset-by-one= quiniela.nombre + " - Posiciones"
    div.row
        if quiniela.participacions.count == 0
            h6 No hay participantes registrados
        else
            table.ten.columns.offset-by-one
                thead
                    tr
                        th Activo
                        th Nombre
                        th 
                tbody
                    each p, key in quiniela.participacionesSurvivor
                        if key < 6 || participacion.id == p.id
                            tr
                                td.center.startd
                                    if p.activo
                                        img(src=url('img/success.png') height="18")
                                    else
                                        img(src=url('img/cancel.png') height="18")
                                td= p.usuario.nombre
                                    if p.no_reponches > 0
                                        span= " (R" + p.no_reponches + ")"
                                td.center
                                    if p.ultimoPronostico.id_equipo_ganador !== null
                                        - url = p.ultimoPronostico.equipoGanador.imagen
                                        img(src=url height="50")
                                    else
                                        span N/J
    .row
        button.ten.columns.offset-by-one.primary-btn(onclick="location.href='"+ url('/quinielas/info/' + participacion.quiniela.id) +"'") Más información
    .micro-space