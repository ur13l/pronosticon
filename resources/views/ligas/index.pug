extend /layouts/app

block title
    title Ligas

block scripts
    script(src=url('/js/ligas/index.js'))

block content
    
    div.container
        h1.center Ligas
        div.row
            input.four.columns#busqueda(type="search" name="busqueda" placeholder="Buscar...")
            //button.three.columns.offset-by-five.primary-color-background(onclick="location.href='"+ url('/ligas/nuevo') +"'") Nueva liga
        div.lista 
            if count(ligas) == 0 
                br
                br
                br
                h5 No hay ligas disponibles
            include /ligas/lista



